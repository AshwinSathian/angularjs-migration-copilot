# Build Progress

This is the single source of truth for where this project actually stands — read it first, before anything else, at the start of any session. It answers two questions: what's built and verified so far, and what's the next concrete thing to do. `docs/decisions.md` is the complementary log of *why* specific technical choices were made; this file is *where things are right now*.

Update this file at the end of every session — rewrite the status table and "Where things stand" section to reflect current reality, and append one entry to the session log. Never leave it describing a past state as if it were current.

## Status at a glance

| Milestone | Status | PR |
|---|---|---|
| M0 — Inventory scanner | done | [#1](https://github.com/AshwinSathian/angularjs-migration-copilot/pull/1) |
| M0.5 — Target workspace scaffold | done | [#3](https://github.com/AshwinSathian/angularjs-migration-copilot/pull/3) |
| M1 — Deterministic codemods | in progress (9/10 patterns) | [#5](https://github.com/AshwinSathian/angularjs-migration-copilot/pull/5), [#6](https://github.com/AshwinSathian/angularjs-migration-copilot/pull/6), [#8](https://github.com/AshwinSathian/angularjs-migration-copilot/pull/8), [#10](https://github.com/AshwinSathian/angularjs-migration-copilot/pull/10), [#11](https://github.com/AshwinSathian/angularjs-migration-copilot/pull/11), [#12](https://github.com/AshwinSathian/angularjs-migration-copilot/pull/12), [#13](https://github.com/AshwinSathian/angularjs-migration-copilot/pull/13), [#15](https://github.com/AshwinSathian/angularjs-migration-copilot/pull/15), [#16](https://github.com/AshwinSathian/angularjs-migration-copilot/pull/16), [#17](https://github.com/AshwinSathian/angularjs-migration-copilot/pull/17), [#18](https://github.com/AshwinSathian/angularjs-migration-copilot/pull/18) |
| M2 — Verification gate | not started | — |
| M3 — LLM fallback + scheduler | not started | — |
| M4 — Web layer | not started | — |
| M5 — Fixtures, licensing, published report | not started | — |

Full scope and definition-of-done for each milestone: `docs/milestones/`.

## Where things stand

**Repo and tooling:** Nx workspace scaffolded (npm, Node 24, TypeScript 6, ESLint 10, Vitest 4 — see `docs/decisions.md` ADR-001–004). CI runs one job (`secrets-scan`, gitleaks) — the rest of the pipeline described in `docs/architecture.md §5` gets added as each milestone lands the tooling it needs (ADR-009). Branch protection on `main` requires a PR and a passing `secrets-scan` check; there is no working direct-push path, by design.

**Built and verified (M0):**
- `libs/secrets-scan` — pattern-based credential detection and redaction, used before any content reaches an LLM prompt.
- `libs/migration-core/src/ingest/` — AngularJS version detection, build-tool detection, structural (AST-based) Karma/PhantomJS detection and remediation-generation, and the repo-wide secrets scan.
- `libs/migration-core/src/inventory/` — an AST scanner (ts-morph) covering module declarations, the full `angular.Module` registration surface (controller/directive/component/service/factory/provider/value/constant/filter/decorator/animation) with DI extraction, ngRoute/ui-router routes, `$watch` usage, and vendored-framework-source exclusion.
- A CLI (`inventory <repoPath>`) wiring both into one JSON report. Not published to npm yet — run locally via `node libs/migration-core/dist/cli.js`.
- `fixtures/angular-phonecat` vendored as a pinned git submodule (commit `ef6f6eb`); its `npm install` and real Karma suite (5/5 specs) have both actually been run on Node 24, not assumed to still work.
- 84 unit tests, all passing. Went through an 8-angle adversarial review before merge that found and fixed several real bugs — see `docs/decisions.md` ADR-010 and ADR-014–019, and the "Adversarial review round" section of `docs/milestones/m0-inventory.md` for the specifics (a couple are worth reading even if you skip everything else: a regex-based remediation that could corrupt unrelated code, and a scanner that briefly mistook AngularJS's own framework source for the application being migrated).

**Built and verified (M0.5):**
- `libs/migration-core/src/scaffold/` — `scaffoldTargetWorkspace()` shells out to the real Angular CLI (`npx @angular/cli@22.1.8 new ...`, subprocess only — no `@angular/*` import, so the module-boundary rule holds) to generate a real Angular workspace into a caller-specified, empty output directory; `verifyWorkspaceBuilds()` runs `ng build` inside it and reports the real exit code. Wired into the CLI as `angularjs-migration-copilot scaffold <outputDir>`.
- Re-verified against the live npm registry (not cached docs, which were a full minor behind): Angular's actual latest stable is `22.1.x`, not `21.x` — see ADR-020. `@angular/cli@22.1.8` pinned as a root devDependency.
- Real, unmocked runs performed and confirmed, not assumed, twice — once pre-review, once after the fixes below: `ng new` generates a real workspace at the exact requested absolute path, `ng build` exits 0, `dist/<name>/browser/` contains real build output.
- Merged only after a 4-angle adversarial review (silent-failure, type-design, test-coverage, general) found and fixed real defects, not style nits — see ADR-021 and ADR-022: `ng new --directory` silently mishandling an absolute path (nests under cwd instead of erroring); `appName`/`angularCliVersion` reaching `spawn`'s argv unvalidated, confirmed exploitable via `ng new` flag-injection and npm's `name@npm:other-package@version` alias syntax; a timeout that only signaled the immediate `npx` process, never the `npm install` grandchild doing the real work; and a directory-emptiness check that silently treated permission errors as "safe to proceed," disabling its own guard exactly when it mattered.
- 99 total unit tests across the workspace (84 in `migration-core`, 15 in `secrets-scan`), all passing; `typecheck` and `lint` clean.

**Built and verified (M1, in progress — 4 of 10 patterns):**
- `mrholek/CoreUI-AngularJS` (pinned `ef0c602`) and `akveo/blur-admin` (pinned `7f2596f`) vendored as pinned git submodules under `fixtures/`, same convention as `angular-phonecat`. `npm install` actually run against both on Node 24: `CoreUI-AngularJS` is a no-op (no dependencies); `blur-admin` fails plain `npm install` for two reasons unrelated to this project's own code (`node-sass` native build, a dead upstream bower tag) — `npm install --ignore-scripts` is the confirmed-working command for it. See ADR-023, `fixtures/README.md`.
- `libs/migration-core/src/codemods/` stood up as the shared plumbing for all class-wrapping patterns — a pure string-in/string-out transform contract (`CodemodResult`, a discriminated union) plus `class-wrapping.ts` (IIFE-safe insertion point, identifier/reserved-word validation, pre-existing-binding check, simple-parameter check with its invariant enforced at the one function that needs it, position-grouped edit application, a generalized `isInsideThisRebindingBoundary` check, and — new this session — `classWrappingSkipReason` and `collectBareFunctionControllerMatches`, the name-validity/duplicate/param tail and bare-function-`.controller`-collection logic shared identically across all three patterns). Pattern #3 (array-style DI → constructor injection, ADR-024), pattern #1 (`$scope.x = y` → class property, ADR-027), and pattern #2 (controllerAs → component class, ADR-029) all implemented and wired into the CLI as `codemod <pattern> <file>`.
- **A real, severe detection gap was found and fixed this session, in already-merged pattern #1, not just the new pattern**: `collectBareFunctionControllerMatches` only recognized an inline function literal as a `.controller` call's second argument, but running pattern #2 against the real `blur-admin`/`CoreUI-AngularJS` fixtures (not just hand-written test fixtures) showed the dominant real-world idiom — 66 of 91 real `.controller` calls across both fixtures, 0 using an inline literal — is `.controller('X', X)`, referencing a separately-declared `function X(...) {...}`. Pattern #1 had been matching **zero** real controllers in either fixture since merge. Fixed at the shared root (`resolveNamedFunctionDeclaration`, real symbol resolution, plus a second real bug where TypeScript's JS binder treats the equally-common `Ctrl.$inject = [...]` annotation as a second "declaration" of the same symbol) — see ADR-030.
- **That first fix was itself broken, caught by adversarial review before merge, not assumed correct from matching test substrings**: replacing a named `function X(...)` declaration with `class X {...}` in the same textual position produces a real compile error (`TS2449: Class 'X' used before its declaration`) whenever the `.controller('X', X)` call precedes the declaration — the dominant real ordering in both fixtures — since `function` hoists but `class` doesn't. A second bug in the same fix left the `Ctrl.$inject = [...]` statement dangling, assigning a property TypeScript doesn't recognize on the now-class binding (`TS2339`). Confirmed by actually running `tsc` against the codemod's own output, not by re-reading the splice logic. Both fixed at the same shared root — the class is now inserted before the call's own statement and the original `function` (plus its `$inject` annotation, if present) deleted, rather than replaced in place — see ADR-031. **Re-verified for real after the fix**: real `tsc` runs against the codemod's actual output for both scenarios show zero `TS2449` and zero `TS2339`-on-`$inject` errors; pattern #1 confirmed 4/4 real hits against `CoreUI-AngularJS`'s `.controller`-bearing files (all now compile past the TDZ/`$inject` class of error); pattern #2 confirmed 11 real hits against `blur-admin`.
- Real per-repo mechanical hit counts (not smoothed, CLAUDE.md's reporting-honesty rule): `angular-phonecat` 0/0 `.controller` calls (uses `.component()` exclusively, ADR-010 — nothing for either pattern to miss); `CoreUI-AngularJS` 4/4 files pattern #1, 0/4 pattern #2 (correctly zero — its controllers are `$scope`-based, not controllerAs); `blur-admin` 13/50 pattern #1, 11/50 pattern #2. (These counts measure detection/matching, not full compilation — the pre-existing, already-accepted gap of undeclared class fields for `this.x = y`/`vm.x = y` body assignments, present since pattern #1/#3's original merge, is M2 verification-gate territory per the same precedent as the factory-return-type case, not something these codemods pre-empt.)
- All three patterns went through **two or more** independent rounds of adversarial review before merge (more than M0/M0.5's single-round practice, at explicit request to go beyond the basics) — pattern #3: 8 real defects across both rounds (ADR-025, ADR-026); pattern #1: 4 more, including a shadow-detection bug found via a hand-rolled ancestor-walk that three separate review agents independently found different holes in, which was the signal to switch to ts-morph's actual symbol resolution instead of continuing to patch cases one at a time (ADR-028); pattern #2: the named-reference detection gap found by running against real fixtures (ADR-030), then a code-review pass on that very fix catching the TDZ/`$inject`/duplicated-`isPlainAssignment` bugs above (ADR-031). Worth reading even briefly: pattern #3's round 2 found classes being hoisted out of the extremely common `(function(){...})()` IIFE wrapper; pattern #1's round 2 found that `$scope` shadowed by a nested `var` (not just a nested function parameter) was silently mistransformed; pattern #2's review found that its own first fix for one severe bug (zero real hits) introduced a second one (broken compile output) that passing tests alone didn't catch.
- **A second, independent review pass on ADR-031's own fix (ADR-032) found three more real issues**: two different `.controller` registrations resolving to the same underlying named `function` could each schedule deletion of its span using stale (pre-edit) offsets, corrupting unrelated trailing content — confirmed by constructing a file with a marker string that visibly got mangled. Fixed by requiring the referenced function's own name to match the registration name, plus an independent `claimedNamedDeclarations` dedup as a second safety net. Separately, `hasScopePropertyAssignment` (pattern #2's "already pattern #1's territory" check) didn't apply the same this-rebinding-boundary exclusion pattern #1's own transform uses, so a controller with a safe top-level `this.x = y` and an unrelated nested `$scope.y = z` was silently missed by *both* patterns — confirmed by running both codemods against the same file and seeing both report no match. Fixed. A third, smaller DRY finding (`resolvesToParam`/`resolvesToOneOf` near-duplicated) was unified into a shared `resolvesUniquelyTo`.
- **Before merge, four review angles ran in parallel against the fully-fixed branch (silent-failure-hunter, type-design-analyzer, pr-test-analyzer, general — same practice as M0/M0.5) and found two more real bugs in the same `$inject`/named-declaration machinery, plus a deeper gap in ADR-031's own claim (ADR-033)**: `findInjectAssignmentStatement` only ever deleted the *first* of multiple `$inject` statements for one function (a second survived as a real `TS2339`), and its ancestor walk accepted an `$inject` assignment nested inside another expression or a braceless `if` body, deleting the wrong (sometimes much larger — an entire IIFE) range instead of refusing to touch it — both confirmed with constructed repros, one via a marker string that visibly got corrupted. Deeper still: ADR-031's "always declared before its only reference" claim was never actually verified — a `.prototype` extension or second registration referencing the same function *before* the matched one resurfaces the exact TDZ bug the whole named-declaration path exists to avoid, confirmed by constructing exactly that case. Fixed together: `findInjectAssignmentStatements` (now plural) collects every match and only accepts a genuine standalone statement; a new `hasOtherReferences` check rejects any candidate with an unaccounted-for reference to its function, which also fully subsumes (and replaces) ADR-032's same-function dedup — two `.controller()` calls sharing a function are now both rejected upstream instead of "transform the first, skip the second," which turned out not to guarantee TDZ safety either. Two cheap type-design fixes applied: `BareFunctionControllerMatch` (and both `Candidate` types) collapsed into a proper discriminated union on its third occurrence of the same loosely-paired fields, and `resolvesUniquelyTo`'s unnecessary union parameter simplified to array-only. The test-coverage gap behind three of ADR-031's original bugs — nothing ever asserted the output actually *compiles* — closed with a shared `assert-compiles.ts` helper (ts-morph diagnostics, no subprocess) wired into every named-declaration/IIFE/`$inject` test.
- 165 total tests workspace-wide (7 more since ADR-032), all passing; `nx run-many -t lint,typecheck,test` clean; real per-repo hit counts against all three fixture repos unchanged (`CoreUI-AngularJS` 4/4 pattern #1 / 0/4 pattern #2, `blur-admin` 13/50 / 11/50) — the stricter checks reject nothing in real fixture code, only the constructed adversarial cases. Merged via [PR #5](https://github.com/AshwinSathian/angularjs-migration-copilot/pull/5), [PR #6](https://github.com/AshwinSathian/angularjs-migration-copilot/pull/6), [PR #8](https://github.com/AshwinSathian/angularjs-migration-copilot/pull/8), and [PR #10](https://github.com/AshwinSathian/angularjs-migration-copilot/pull/10).
- **Pattern #3's own flagged-but-unchecked gap (previous session's own "What's next" note) confirmed real and fixed this session (ADR-034)**: `array-di-to-constructor.ts`'s array-last-element check only recognized an inline function/arrow literal, rejecting the equally-valid `['$http', SomeIdentifier]` named-reference shape — the same gap ADR-030 found and fixed for bare-function `.controller` registrations, now confirmed to generalize to array-style DI too. Fixed by exporting and reusing (not re-deriving) `resolveNamedFunctionDeclaration`/`findInjectAssignmentStatements`/`hasOtherReferences` from `class-wrapping.ts`. **Real-world impact actually checked, not assumed**: ran the fixed codemod against all 66 registration-bearing files across all three vendored fixtures — zero real array-style DI registrations exist in any of them (`CoreUI-AngularJS`/`blur-admin` both use bare-function DI with `$inject` annotations exclusively), so pattern #3's hit count against this project's own fixtures stays 0/66, honestly reported rather than omitted; this was also the first time pattern #3 was checked against the real fixtures at all, not just its own hand-written test fixtures. **A first adversarial review round on this fix (`/code-review high`) found a real, severe corruption bug it introduced, then traced to a root cause predating this session**: a registration call written inside the very function it registers is that function's only reference, so `hasOtherReferences` alone accepted it, but the class-insertion point then fell inside the function's own deleted range — `applyEdits` spliced both as if disjoint, corrupting output while still reporting `matched: true`. Confirmed this pre-existing gap also silently corrupts already-merged, already-shipped pattern #1 (`scope-assignment-to-class-property.ts`, which shares `hasOtherReferences`) by actually running the same repro against it. Fixed at the shared root in `hasOtherReferences`, not pattern-locally. A second, independent review round found no further functional issues, only a documentation-process gap (this entry and ADR-034 close it).
- **A third review round, run against the open PR before merge, found round 1's shared-root fix was itself too narrow (ADR-035)**: `hasOtherReferences`'s guard only rejected a function referencing *itself* from inside its own body, not a candidate whose body nests a *different* registration referencing a *different* function — confirmed by actual execution to corrupt output in both pattern #3's new path and already-merged pattern #1. Fixed with a more general shared check, `findNestedDeletionConflicts` (`class-wrapping.ts`): after all candidates for a run are known, it rejects any candidate whose deletion range strictly contains another candidate's edit positions, wired into both `collectBareFunctionControllerMatches` (patterns #1/#2, filters silently per that function's own convention) and pattern #3's own candidate list (surfaces a warning per its own convention). Also closed a real coverage gap: pattern #2 (`controlleras-to-class.ts`) shares the identical mechanism but had no regression test for either nesting case, despite being just as exposed. One incidental finding: a correctly-untouched original `function MainCtrl() { this.x = 1; }` legitimately trips `TS2683` under `assertCompiles`'s strict context — not a codemod defect (only a plain, untransformed function's `this` is ever untyped; a codemod-emitted class's members always are), added to the ignore list with the same reasoning already established for 2339/2304. 9 new test cases across the three affected spec files (4 named-reference cases, 2 self-registration regressions, 3 cross-candidate nesting regressions — one per affected pattern spec), `assertCompiles` used in every new test.
- **A fourth round — a four-agent `/code-review high` swarm run against the updated PR before merge — converged independently on one more gap in the same fix (ADR-036)**: `findNestedDeletionConflicts` still only treated a named declaration's deletion range as a possible container, not an *inline* function/arrow literal's own replaced span — which destroys exactly the same text once replaced by the bare class name wholesale. Two of the four agents confirmed by direct execution against the actual committed code (one via an isolated scratch snapshot, specifically to rule out interference from this fix already landing concurrently in the working tree) that an inline outer candidate nesting a second, unrelated registration silently produced mid-token-sliced corrupted output (e.g. `Outerontroller('Inner', Inner);`) with `matched: true` and no warning. Fixed by generalizing `NestingCheckItem` to a required `deletedRanges` list covering every candidate's own deleted-or-replaced span, named or inline alike. A fourth agent explicitly traced through and ruled out, as not a real bug, a structural question a different agent had raised in an earlier pass (pattern #3's named-declaration branch not requiring the resolved function's own name to match the registration name) — confirmed safe by design given the array-replacement strategy, not an oversight, after deliberately constructing and testing both cases the sibling code's own docstring calls out. Removed a leftover untracked scratch spec file one review agent left behind during its own investigation. 178 tests passing workspace-wide (3 more this round), `nx run-many -t lint,typecheck,test --skip-nx-cache` clean.
- **A fifth review round — another multi-agent swarm — found one more real, low-severity gap (ADR-037)**: `collectBareFunctionControllerMatches`'s silent nesting-conflict filtering left both bare-function callers unable to tell "the idiom wasn't there" apart from "the idiom was there but its only real candidate got rejected for an unrelated reason," both reporting the same generic "not found" message. Fixed by returning which class names were dropped and why (`{ matches, nestingConflictClassNames }`), and a shared `buildNoMatchReason` helper so both patterns' fallback message honestly reflects which case actually happened. Two other findings from the same round were investigated and deliberately left as-is: the array-di named-declaration branch's asymmetric name-match requirement (re-confirmed safe by a second independent agent; a clarifying comment added) and `NestingCheckItem`'s hand-duplicated construction at its two call sites (a real but premature abstraction question, deferred per this project's own "rule of three" precedent). A sixth round found and fixed a real ordering bug (ADR-038): pattern #3's name-deduplication ran *before* nesting-conflict filtering, so a doomed candidate still claimed its name and wrongly blocked an unrelated, valid later registration sharing it — fixed by deferring dedup to after filtering. `assert-compiles.ts`'s 2683-suppression comment was also corrected (it claimed emitted-class members never trigger 2683; a direct probe showed a verbatim-copied nested callback does). PR #11 merged. 180 tests passing.

**Built and verified (M1, pattern #4)**: `libs/migration-core/src/codemods/directive-to-component.ts` — simple directive (`.directive` only, not `.component()` — deferred, different DDO shape) → `@Component`/`@Directive` (ADR-039). Real-fixture evidence checked before writing any code: the same named-reference idiom ADR-030 found dominant for `.controller` is 100% of real `.directive()` calls too, so that machinery was reused from day one rather than rediscovered through another multi-round review cycle. Supports both real factory-body shapes (`return {...};` and `CoreUI-AngularJS`'s dominant `var directive = {...}; return directive;`). Skips (documented, not guessed at): `transclude`/isolate `scope` when truthy (scope bindings are pattern #9's job), `compile`/`link` on presence alone (every real fixture `link` function does raw DOM manipulation with no safe mapping), a string/non-function `controller` (the dominant real shape — cross-file resolution is out of scope for every codemod in this project, all single-file by design), and a factory body that does more than return a DDO literal. **Deliberately departs from patterns #1–#3's architecture**: never deletes or replaces the original `.directive(...)` call or factory — a `@Component`/`@Directive`-decorated class isn't valid AngularJS registration syntax, so there's no in-place swap; the new class is inserted alongside the untouched original instead. This sidesteps pattern #3's entire nesting-conflict bug class (ADR-034–038) by construction — nothing is ever deleted, so there's no deletion range for a sibling's edits to nest inside. Real per-repo hit counts: `angular-phonecat` 0/0 (`.component()`-exclusive), `CoreUI-AngularJS` 0/2 (both files are `link`-only), `blur-admin` 3/35 — all three real hits manually verified end-to-end (correct selector, correctly inserted inside the enclosing IIFE), every skip reason spot-checked against the actual fixture source. 18 total new tests (198 total workspace-wide), `assertCompiles` used throughout — decorator syntax compiles cleanly under the existing ignored-code set with no changes needed. A self-review pass on the open PR (ADR-040) found and fixed two real silent-data-loss bugs (method-shorthand `controller`/`template` silently treated as absent) and one selector-classification bug before merge — neither reachable by real fixture code, both found by adversarial construction. Merged via [PR #12](https://github.com/AshwinSathian/angularjs-migration-copilot/pull/12).

**Built and verified (M1, pattern #9)**: `libs/migration-core/src/codemods/bindings-to-input.ts` — one-way `bindings: { x: '<' }` → `@Input()` (ADR-041). Scoped to `.component()` registrations only, not `.directive()`'s isolate `scope`: real-fixture evidence (checked before writing any code, same standing practice as every prior pattern) showed all 7 isolate-`scope` directives across the fixtures (all in `blur-admin`) also have a `link` function pattern #4 already unconditionally skips, and none use one-way (`'<'`) bindings anyway — the directive-scope path ADR-039 gestured at was deliberately not built this session, flagged rather than silently dropped. Same insert-only architecture as pattern #4 (new class inserted alongside the untouched original `.component(...)` call), reusing `toPascalCase`/`toKebabCase`/`getObjectLiteralProperty` exported from `directive-to-component.ts` rather than re-deriving them.
- **Two independent `/code-review high` passes against the open PR (explicit "go beyond the basics" instruction), each confirmed by direct execution, found four real bugs before merge (ADR-042)**: a duplicate `bindings` key emitted two identical `@Input()` fields (`TS2300`, `matched: true`); a non-identifier binding key (a quoted `'my-attr'`) was emitted verbatim as an invalid class field name; `usedClassNames.add()` ran before `template`/`templateUrl` validation could still fail, so a skipped registration permanently blocked a later, valid one sharing its derived class name — a regression against pattern #4's already-correct ordering; and `assert-compiles.ts`'s new `2552` ignore was a bare code (too broad, would mask any future near-miss-spelling bug, not just the known `Input`/`oninput` case) — narrowed to a message-scoped ignore. Also extracted `extractTemplateProp` (`directive-to-component.ts`), closing a DRY gap the same review flagged. A third review round found no further issues — converged. 5 new tests (`assert-compiles.spec.ts` added as its own file), 215 tests passing workspace-wide, real fixture hit counts unchanged.
- Real per-repo hit counts, honestly reported: `angular-phonecat` 0/2 (both real `.component()` calls have no `bindings` at all, confirmed by running the actual CLI against both files), `CoreUI-AngularJS` 0/0, `blur-admin` 0/0 (neither uses `.component()` at all) — an honest 0/0/0/0, not a bug, given this pattern's own literal, narrow definition and how little `.component()` usage exists in these particular fixtures. `assertCompiles` used throughout every test, `nx run-many -t lint,typecheck,test --skip-nx-cache` clean. Merged via [PR #13](https://github.com/AshwinSathian/angularjs-migration-copilot/pull/13).

**Built and verified (M1, pattern #5)**: `libs/migration-core/src/codemods/filter-to-pipe.ts` — a pure `.filter(name, factory)` → Angular `@Pipe` (ADR-043). Real-fixture evidence checked before writing any code (standing practice): all 5 real `.filter()` registrations across the fixtures (all in `blur-admin`) use the named-reference idiom ADR-030 found dominant elsewhere — `resolveNamedFunctionDeclaration` reused as-is. A factory body must be exactly `return function (...) {...};` (one statement); anything else is a documented skip, same conservative treatment pattern #4 gives a multi-statement DDO body. Same insert-only architecture as patterns #4/#9 — confirmed, not assumed, before committing to it. **A correctness issue specific to this pattern**: the returned function becomes a `transform` *method*, not the constructor body, so a bare reference to one of the factory's own DI parameters inside it (the real shape in 3 of `blur-admin`'s 5 filters) needs rewriting to `this.<name>` or it silently breaks once lifted out of its original closure — done via the same `isInsideThisRebindingBoundary` guard used elsewhere, skipping the whole registration when a reference sits inside a nested non-arrow function/accessor. Real per-repo hit counts as originally reported at merge time: `angular-phonecat` 0/0, `CoreUI-AngularJS` 0/0, `blur-admin` 5/5 — all 5 real hits manually verified via the real CLI, including the 3 closure-rewrite cases and the original registration/factory left untouched. **The `angular-phonecat` 0/0 was itself wrong, corrected 2026-09-21 (ADR-045)**: the evidence-gathering grep for the literal text `.filter(` missed `fixtures/angular-phonecat/app/core/checkmark/checkmark.filter.js`'s line-broken `angular.\n  module('core').\n  filter(...)` chaining style — a real, in-scope registration the codemod's own AST-based detection already transforms correctly, no code change needed. Corrected count: `angular-phonecat` **1/1**, `CoreUI-AngularJS` 0/0, `blur-admin` 5/5. **Three `/code-review high` rounds run before merge (per explicit "go beyond the basics" instruction), converging on the third**: round 1 found a self-recursive named-function-expression factory (`return function step(input) { ...step(input-1)...; }`) was silently accepted despite the returned function's own name being unresolvable once lifted out of its named-expression scope — fixed with a new `hasSelfReference` skip. Round 2 found a closed-over DI parameter referenced via ES6 object-literal shorthand (`{ layoutPaths, input }`) was silently *not* rewritten — `resolvesUniquelyTo` (`class-wrapping.ts`, shared with patterns #1/#2) resolved a shorthand identifier's symbol to the `ShorthandPropertyAssignment` itself, not the referenced parameter; a `matched: true`-but-wrong bug in the exact class ADR-040 warns about. Fixed at the shared root using ts-morph's `getValueSymbol()`, confirmed by a probe script, and confirmed (by tracing every other caller) not to change patterns #1/#2's own behavior — their callers can never pass a shorthand-positioned identifier in the first place. The same round also fixed the rewrite itself: a shorthand property can't take `this.<name>` as its shorthand form, so the replacement expands to the full `<name>: this.<name>` there. Round 3 found nothing new. 15 tests, `assertCompiles` used throughout, `nx run-many -t lint,typecheck,test --skip-nx-cache` clean, real fixture hit counts unchanged after both fixes.

**Built and verified (M1, pattern #8)**: `libs/migration-core/src/codemods/routes-to-router-config.ts` — flat `$routeProvider`/ui-router `$stateProvider` route states → an Angular `Routes` array (ADR-044), merged via [PR #16](https://github.com/AshwinSathian/angularjs-migration-copilot/pull/16). Real-fixture evidence checked before writing any detection code corrected a stale claim from the prior session's own resume brief: `CoreUI-AngularJS` is **not** 0/0 for routing — it has 27 real `.state()` calls, all correctly out of scope (every flat name is `abstract: true`), so `0/3` is the honest count. Real per-repo hits: `angular-phonecat` 1/1, `blur-admin` 2/26, `CoreUI-AngularJS` 0/3. More rigorous than `inventory/scan-routes.ts`'s receiver-text-matching by design — resolves the real `$routeProvider`/`$stateProvider` parameter via array position or parameter name (plus a `$inject`-annotation fallback added during review), then walks `.when`/`.state`/`.otherwise` calls via real symbol resolution. Same insert-only architecture as patterns #4/#9/#5. Self-review before opening the PR found and fixed one real bug (an unvalidated derived `Routes` constant name). An 8-angle `/code-review high` swarm (6 of 8 angles completed — 2 hit an unrelated provider rate limit) found two more real bugs, both fixed with regression tests: **HIGH** — `.otherwise()`'s wildcard entry was emitted at its source position instead of forced last, which (unlike AngularJS's own always-evaluated-last semantics) would let it shadow every Angular `Routes` entry after it; **MEDIUM** — the provider-parameter lookup missed the real, minifier-safe `fn.$inject = [...]` annotation idiom entirely, silently failing to recognize an otherwise-valid routing config. Several DRY fixes landed in the same pass, two of them into shared infrastructure other patterns now reuse too: `isTruthyValue` exported from `directive-to-component.ts`, `resolveChainRoot` and `arrayDiArityMismatchReason` moved into `inventory/ast-helpers.ts` (the latter now also used by `array-di-to-constructor.ts`, replacing a duplicated message). One pre-existing gap shared with patterns #4/#9/#5 was flagged, not fixed: `hasExistingTopLevelBinding` only checks file-top-level bindings, not the enclosing IIFE an insertion actually lands in — see ADR-044 and the "Known, open, non-blocking" list below. 20 tests, `assertCompiles` used throughout, 251 tests passing workspace-wide, `nx run-many -t lint,typecheck,test --skip-nx-cache` clean.

**Built and verified (M1, pattern #10)**: `libs/migration-core/src/codemods/event-bus-to-subject.ts` — `$emit`/`$broadcast`/`$on` within one module → a typed RxJS `Subject` service (ADR-046), the last of the 10 fixed v1 codemod patterns. Real-fixture evidence checked before writing any detection code (standing practice since ADR-030/034/039/041/043/044): every real `$emit`/`$broadcast`/`$on` call across all three vendored fixtures (2 in `CoreUI-AngularJS`, 7 in `blur-admin`, 0 in `angular-phonecat`) is either a `$destroy` lifecycle-cleanup listener, a ui-router-internal `$stateChangeSuccess`/`$stateChangeStart` broadcast (never sent by app code itself), or one orphaned `$broadcast('fileProgress', ...)` with no listener — zero real occurrences of the pattern's actual target shape (an event name both sent and received by app code within one module) exist in any fixture, confirmed by running the real CLI against every one of the 8 real files with a matching call, not just a grep. Given the total absence of real-fixture evidence and this pattern's real architectural divergence from every prior insert-only pattern (there's no single registration call to attach to — the source calls are scattered `$scope`/`$rootScope` method calls, and a *complete* migration would also need to rewrite each call site, a fundamentally riskier class of edit), the scope was confirmed with the user before writing any code: an insert-only scaffold (one `EventBusService` per file, a typed `Subject<unknown>`-backed member trio per paired event name), original call sites left untouched, call-site rewiring deliberately out of scope. **A first adversarial review round found one severe, confirmed-by-execution bug**: reusing `nearestInsertionPointStart` (class-wrapping.ts) by analogy to every prior pattern — built for a registration call already sitting directly at module top level — landed the generated class *inside* whatever closure happened to contain the (often deeply-nested) `$emit`/`$on` call instead, e.g. inside a `$scope.notify = function () {...}` closure: redeclared on every invocation, unreachable from the rest of the module, `matched: true`. Fixed with a new, pattern-local `moduleTopLevelInsertPos` — `EventBusService` has no constructor parameters and closes over nothing, so unlike every other insert-only pattern's class it has no need to stay inside an enclosing IIFE at all; it's always inserted at the true top of the file, after any leading `'use strict'` directive-prologue statement. **A second review round found two more real, confirmed bugs**: the reserved-word check in `isValidClassName` was wrongly applied to a derived member-name base that's never emitted bare (always suffixed/prefixed, e.g. `deleteSubject`/`emitDelete` — both valid even though `delete` alone is reserved) — fixed by checking the character-class-only `VALID_IDENTIFIER` regex instead; and the collision check only guarded the derived `EventBusService` class name, never `Subject`/`Injectable` (the names the generated class body actually references), so a pre-existing top-level binding with either name would silently shadow the real rxjs/`@angular/core` names — fixed by checking all three. **A third review round (`/code-review ultra`, against the open PR, per explicit "go beyond the basics" instruction) found two more real bugs**: detection matched any `x.$emit(...)`/`x.$on(...)` call by method name alone with no receiver check, confirmed to false-positive on an unrelated object that merely exposed same-named methods — fixed by restricting to the exact receiver names every real fixture uses (`$scope`/`$rootScope`/`scope`/`rootScope`); and the `Subject`/`Injectable`/`EventBusService` collision check (`hasExistingTopLevelBinding`, class-wrapping.ts) never checked `import` bindings, only declarations — confirmed a pre-existing `import { Subject } from './my-custom-subject';` silently shadowed the real rxjs `Subject`, `matched: true`, no compile diagnostic to catch it — fixed at the shared root (a new `importLocalBindingNames` helper), same failure class ADR-045 already fixed for this function's IIFE-scope blind spot, just a different blind spot in it. This is a shared-infra change affecting every insert-only pattern; full workspace suite re-run clean, real fixture behavior re-verified unchanged for pattern #4 and pattern #10 (no fixture uses ES `import` syntax). Real per-repo hit counts, honestly reported: `angular-phonecat` 0/0, `CoreUI-AngularJS` 0/2, `blur-admin` 0/7 — an honest 0/0/0, same accepted-low-number precedent as pattern #9's own 0/0/0/0 (ADR-041). 22 tests, `assertCompiles` used throughout, 275 tests passing workspace-wide, `nx run-many -t lint,typecheck,test --skip-nx-cache` clean. Not fixed, flagged for later: `cli.ts`'s shared warning label is mildly inaccurate for this pattern's own skip reasons (pre-existing generic wording shared by every codemod, out of scope here); a template-literal event name and non-ASCII characters in an event name aren't recognized/preserved — zero real-fixture evidence for either, documented gaps rather than guessed-at fixes.

**Built and verified (M1, pattern #6)**: `libs/migration-core/src/codemods/http-then-to-httpclient.ts` — `$http` + `.then()` chains, depth ≤ 2, → `HttpClient` + RxJS (ADR-047). Real-fixture evidence checked first: `$http` is barely used across the fixtures at all — `CoreUI-AngularJS` has zero references, `angular-phonecat` uses `$resource` exclusively (zero real `$http` hits), `blur-admin` has exactly 2 real call sites (both in `WeatherCtrl.js`), whose success/error callbacks are inline named function expressions heavily entangled with `$scope` and sibling closure functions. Given that real entanglement — a genuine step up in risk from every prior pattern — the scope was confirmed with the user before writing any code: a **signature-only scaffold**. It generates a typed `HttpClient`-based method (verb, URL, an optional `params`/`body` parameter) for each qualifying `$http(...).then()`/`.catch()` chain, but never copies or rewrites the callback bodies themselves — that logic is left as a documented, out-of-scope gap for manual migration. Same insert-only, module-top-level-insertion architecture as pattern #10 (no closure dependency in the generated class, so no need to stay inside an enclosing IIFE); `.jsonp(...)` is a documented whole-match skip (needs `HttpClientJsonpModule` and a different call signature, a real structural API difference). **Self-review before opening any PR found and fixed two real bugs**: the derived URL was embedded into the generated string literal with no escaping (a URL containing a single quote produced a real syntax error) — fixed with `escapeSingleQuoted`; and `resolveStaticString` never checked whether a resolved `method`/`url` variable is reassigned anywhere else in the file (`getDeclarations()` only reports declaration sites) — `var method = 'GET'; method = 'POST';` silently resolved to `'GET'`, a `matched: true`-but-wrong bug — fixed by rejecting any candidate with a plain-assignment reference anywhere in the file. **An `ultra` adversarial review round against the open PR (per explicit "go beyond the basics" instruction) found five more real bugs**: `hasParamsConfig` misread a body-bearing verb's 2-arg call (`post(url, data)`, no config) as having a trailing config object whenever the body literal happened to contain a `params` key — fixed by requiring the right minimum argument count per verb before treating the last argument as config; the reassignment guard only caught plain `=`, not compound operators like `+=` — fixed to check every assignment-family operator; a derived method name colliding with the generated class's own `http` constructor parameter or the reserved `constructor` member broke compilation outright (confirmed via `assertCompiles`) — fixed with a reserved-name check; `body: unknown` was added purely from the verb, forcing a real no-payload trigger POST to expose a meaningless body parameter — fixed by tracking whether the original call actually had body data, passing `null` positionally when a body-bearing verb has none (`HttpClient.post`'s real signature requires *some* body argument); and the scaffolded `params?: Record<string, unknown>` type isn't assignable to Angular's real `HttpClient` params option type — fixed by narrowing to `Record<string, string | number | boolean>`. **A fourth review round on the updated PR found two more real bugs**: `hasBody` was set from a `data` property's presence alone, not from whether the verb was actually body-bearing — a config-object GET with a stray `data` key scaffolded a `body: unknown` parameter declared but never passed to `this.http.get(...)`, silently dropping anything a caller supplied there — fixed by requiring both conditions together; and the "no safe method name" skip message always rendered in shorthand syntax even for a config-object match, misrepresenting a form not actually present in the file being diagnosed — fixed with a `sourceKind` field and a message renderer matching the real source syntax. **A fifth review round found six more real bugs (fixed) and flagged two narrow, zero-evidence gaps (not fixed)**: unescaped line-terminator characters in a URL could break the emitted string literal (fixed, `escapeSingleQuoted` now covers `\n`/`\r`/U+2028/U+2029, not just `\`/`'`); both URL-resolution checks used `if (!url)` instead of `=== undefined`, wrongly rejecting a legally-resolved empty-string URL; `findEnclosingFunction` didn't stop at method/accessor boundaries, so a call inside an ES6 method-shorthand property got attributed to an unrelated outer function; an unrecognized-but-chained shorthand verb (a plausible typo) surfaced no skip reason at all; the config-object form didn't recognize `method: 'JSONP'` the way the shorthand `.jsonp()` form did; and `RESERVED_METHOD_NAMES` hardcoded `'http'` independently of the actual emitted constructor-parameter name, a single-source-of-truth gap. Flagged, not fixed: a destructuring/`for`-loop reassignment isn't caught by the reassignment guard, and `chainLinkCount` doesn't see through a `ParenthesizedExpression` — both real in principle, zero real-fixture evidence for either. Real per-repo hit counts, honestly reported and re-verified unchanged across every round: `angular-phonecat` 0/0, `CoreUI-AngularJS` 0/0, `blur-admin` 1/2 (`updateWeather` scaffolded; the `jsonp` call correctly surfaced as a documented skip warning). 28 tests, `assertCompiles` used throughout, 303 tests passing workspace-wide, `nx run-many -t lint,typecheck,test --skip-nx-cache` clean.

**Not built yet:** 1 remaining codemod pattern (M1 — #7: `ng-repeat`/`ng-if`/`ng-show` → `@for`/`@if` control flow, the only remaining pattern operating on HTML templates rather than TS/JS AST), the verification gate (M2 — the component this whole project's credibility rests on), LLM-assisted fallback (M3), the NestJS/Angular/Mongo web layer (M4), and the final fixture runs with published numbers (M5).

**Known, open, non-blocking:**
- A Dependabot PR may exist proposing an `nx` downgrade to fix the `smol-toml` advisory (ADR-012). Don't merge it without re-deriving the same tradeoff ADR-012 already made — downgrading `nx` was deliberately rejected once already, for reasons that don't go away just because Dependabot found the same CVE.
- `scan-routes.ts`/`scan-watches.ts` don't resolve identifier aliasing (ADR-019) — a documented, low-prevalence gap, not a TODO.
- The four `inventory/scan-*.ts` files still each do their own traversal of the parsed project (ADR-019) — fine at M0's scale, worth revisiting if M1's larger fixtures show it mattering.
- Pattern #3's manual position-tracked text-splice approach (vs. ts-morph's own `Node#replaceWithText`, used elsewhere in this repo) is a real style inconsistency, deliberately not reconciled under review pressure (ADR-025) — worth revisiting once a future pattern's needs make the inconsistency actually matter.
- A `.factory` whose array-style-DI body returns an object with real properties will produce a constructor `tsc` rejects — expected, by design: that's the verification gate's (M2's) job to catch and tier as REJECTED, not something pattern #3 pre-empts.
- The `RawMatch`/`Candidate`/collect→sort→validate→build duplication flagged after pattern #1 (ADR-028) was resolved this session, per its own "revisit once a third pattern shows the actual shape" instruction: pattern #2 confirmed that the name-validity/duplicate/simple-parameter tail (`classWrappingSkipReason`) and the bare-function-`.controller`-collection loop (`collectBareFunctionControllerMatches`) generalize cleanly across all three patterns, and both are now shared in `class-wrapping.ts`. Detection logic and internal-rewrite logic still differ per pattern and were deliberately left pattern-local — see ADR-029.
- Pattern #5's own real hit count against `angular-phonecat` was wrong since it was first documented (ADR-043) and stayed wrong through PR #15's merge — not `0/0`, actually `1/1`: `fixtures/angular-phonecat/app/core/checkmark/checkmark.filter.js` is a real, in-scope `.filter()` registration the codemod itself already transforms correctly. The prior session's evidence-gathering grepped for the literal text `.filter(`, which fails on this file's line-broken `angular.\n  module('core').\n  filter(...)` chaining style — the same style used throughout this fixture, including `app.config.js`. Fixed in `docs/PROGRESS.md` (this entry); `docs/decisions.md`'s ADR-043 itself is left as originally written per the append-only/never-rewrite-past-entries rule — see ADR-045. **Worth generalizing**: any future fixture-evidence grep in this project should account for `angular-phonecat`'s pervasive `x.\n  y().\n  z(...)` chaining style, or better, verify hit counts by running the real CLI against every file rather than pre-filtering with a text grep at all.
- ~~`hasExistingTopLevelBinding` (`class-wrapping.ts`) only checks bindings at the source file's top level~~ — **fixed** (ADR-045): broadened to search every declaration in the file, not just top-level ones, after a second adversarial review round confirmed by direct execution that a same-named binding inside the same IIFE an insert-only pattern inserts into went undetected, producing both a silently-wrong component reference and a real `TS2451` compile error. Affected every insert-only pattern (#4/#9/#5/#8); not reachable by any real fixture; re-verified unchanged real fixture hit counts for all four after the fix.
- Pattern #10's `EventBusService` doesn't recognize a template-literal event name (`` `ready` ``, no interpolation) or preserve non-ASCII characters when sanitizing an event name into an identifier (silently dropped instead of transliterated) — ADR-046, both flagged rather than fixed, zero real-fixture evidence for either shape.
- `cli.ts`'s shared warning label ("Warnings (other registrations in this file left untouched)") is mildly inaccurate for pattern #10's own skip reasons, which are about event channels excluded from one generated service, not sibling registrations left untouched — pre-existing generic wording shared by every codemod (ADR-046 flagged, didn't fix — out of scope for a single-pattern change).

## What's next — prompt for the next session

**Immediate next step**: pattern #7 (`ng-repeat`/`ng-if`/`ng-show` → `@for`/`@if` control flow) is the last remaining M1 pattern. Confirm pattern #6 (this session, ADR-047) is actually merged to `main` before starting (check `git log`/open PRs, don't assume from this file alone).

Continue **M1 — Deterministic codemods** (`docs/milestones/m1-codemods.md`). Patterns #1–#6, #8, #9, and #10 done; only #7 remains. **#7 operates on HTML templates, not TS/JS AST — every pattern so far has been ts-morph over `.js` source; #7 needs a different parser entirely (an HTML parser, not ts-morph) and a different fixture-evidence approach (grep/inspect `.html` template files across the three fixtures, not `.js` files).** Check what templating approach each fixture actually uses before assuming `ng-repeat`/`ng-if`/`ng-show` directive-attribute syntax is the only real shape present — `angular-phonecat` may use component templates differently than `CoreUI-AngularJS`/`blur-admin`'s more classic controller+partial-HTML structure. This is new territory for this project; expect to make and document real tooling/architecture decisions in `docs/decisions.md`, not just detection-logic ones.

Read `docs/product-spec.md §6.3`, `docs/decisions.md` ADR-024/ADR-027/ADR-029/ADR-030/ADR-039/ADR-041/ADR-042/ADR-043/ADR-044/ADR-045/ADR-046/ADR-047, and `libs/migration-core/src/codemods/class-wrapping.ts` first (the last is likely *not* directly reusable for #7's HTML-based detection, but its "skip when ambiguous," insert-only, and real-fixture-evidence-first philosophies all still apply). `hasExistingTopLevelBinding` (class-wrapping.ts) now checks every declaration in the file, including `import` bindings, not just top-level declarations (ADR-045/046) — a real bug fix affecting every insert-only pattern, already re-verified against all five affected patterns; no action needed for a TS/JS-based pattern, just be aware the guarantee is real (irrelevant for #7's HTML-based detection). `resolveNamedFunctionDeclaration`/`findInjectAssignmentStatements`/`hasOtherReferences`/`findNestedDeletionConflicts` remain exported and reusable for any future TS/JS-based pattern. `isInsideThisRebindingBoundary` (class-wrapping.ts) remains available for any closure-reference hazard a future TS/JS pattern hits. **`nearestInsertionPointStart` (class-wrapping.ts) is *not* a safe default to reach for by analogy for a pattern whose matched node isn't a module-top-level registration call** — patterns #10 and #6 both needed a different, pattern-local `moduleTopLevelInsertPos` after #10 first reused the wrong one and produced a real, severe bug (class nested inside an unrelated closure, unreachable — caught by adversarial review, not self-review, ADR-046). Now duplicated in two files (`event-bus-to-subject.ts`, `http-then-to-httpclient.ts`) — a third real user (unlikely for #7, given its HTML-based nature) would be the signal to finally promote it into `class-wrapping.ts`. `directive-to-component.ts` now exports `toPascalCase`/`toKebabCase`/`getObjectLiteralProperty`/`isTruthyValue`/`extractTemplateProp` — reuse rather than re-derive for any future TS/JS pattern. ADR-041 flagged (not built) a `.directive()` isolate-scope path for pattern #9. ADR-046 flagged (not built) template-literal event-name recognition and non-ASCII identifier sanitization for pattern #10. ADR-047 flagged (not built, and likely will stay that way) copying/rewriting `.then()`/`.catch()` callback bodies for pattern #6 — a deliberate scope decision confirmed with the user, not an oversight; revisit only if a future session is explicitly asked to go further than signature-only.

**Check real fixture shapes before writing any pattern's detection logic, not after** — this is now standing practice (ADR-030, ADR-034, ADR-039, ADR-044, ADR-046, ADR-047 all found or confirmed real-world shapes by grepping the vendored fixtures first, then *actually running the CLI* against every real file with a plausible match rather than trusting the grep alone), not optional due diligence. Two patterns in a row (#10, #6) turned up **near-zero or zero** real hits — an honest, expected outcome per `docs/milestones/m1-codemods.md`'s own "a low number here is correct, not a bug" note, not a signal something was built wrong, but also a signal to keep evidence-gathering rigorous rather than assume a pattern will find plenty once built.

**Scope-confirmation guidance, reinforced twice this session**: when a pattern's real-fixture evidence reveals genuine architectural risk (callback bodies entangled with closures/`$scope` that can't be safely lifted out, no clean insert-only attachment point, etc.), confirm the scope with the user directly before writing detection code rather than guessing at how ambitious to be — this happened for both #10 (call-site rewriting vs. insert-only scaffold) and #6 (signature-only vs. full-body-copy vs. defer entirely), and in both cases the user's answer materially shaped what got built. Don't treat "the user said keep going" as license to make an ambitious architecture call solo when the fixture evidence itself is thin and the options are meaningfully different in risk.

**Review-round guidance, reaffirmed again this session**: self-review first, then at least one broader adversarial pass before merge. Pattern #6's self-review (before any PR existed) caught two real bugs (unescaped URL; a reassigned `method`/`url` variable resolving to its stale value) — but that was nowhere near enough on its own: PR #19 then went through **five** adversarial review rounds before merge (per explicit "go beyond the basics" instruction), finding 13 more real bugs total, narrowing each round from severe (a config-object body silently split into a bogus `params` argument, a derived method name colliding with the generated class's own `http`/`constructor` members) to increasingly edge-case (unescaped line terminators, empty-string URLs, a hardcoded reserved-name string). Pattern #10 (PR #18) needed a similar two-round arc. Don't assume self-review or even one adversarial round means convergence when explicitly asked to go beyond the basics — keep going until a round finds nothing but narrow, zero-evidence edge cases (which is what finally ended pattern #6's review), and use judgment on when that point is reached rather than a fixed round count.

Pattern #7 is the last of M1's fixed v1 scope (`docs/product-spec.md §6.3`) — once it lands, M1 itself is done and M2 (the verification gate) is next.

Per CLAUDE.md's scope-discipline and reporting-honesty rules: hit-rate and characterization-eligibility numbers (the latter is M2's job, but keep the per-artifact-type breakdown requirement in mind now since it shapes what metadata each codemod should emit) must never be a single smoothed aggregate — report per-repo and, once M2 exists, per-artifact-type.

As with every milestone here: ends in a PR, not a direct commit to `main`; nothing is "done" until actually run and its output checked, not inferred from the code looking right. Before closing the session, update this file's status table, "Where things stand," and session log.

## Session log

Append one entry per session, newest last. Keep each entry to a few lines — this is a changelog, not a transcript; `docs/decisions.md` and the milestone docs carry the detail.

- **2026-09-15** — Repo created from scratch: product spec and architecture doc adversarially reviewed and rewritten for public consumption (license verification, current-defaults verification, ADR-001–009), GitHub repo pushed with branch protection and CI. M0 implemented (`libs/secrets-scan`, `libs/migration-core` ingest + inventory, CLI), then put through an 8-angle adversarial review that found and fixed real bugs before merge (ADR-010, ADR-014–019). Merged via PR #1. This file created to track progress going forward.
- **2026-09-15** — M0.5 implemented: re-verified Nx/Angular/Node/TypeScript compatibility against the live npm registry, correcting a stale assumption that Angular 21 was current (it's 22.1.x — ADR-020). Built `libs/migration-core/src/scaffold/` (subprocess-only Angular CLI invocation, no `@angular/*` dependency added to `migration-core`), wired a `scaffold` CLI command, added `@angular/cli@22.1.8` as a root devDependency. Real, unmocked run performed: found and fixed a genuine bug where `ng new --directory` mishandles absolute paths (ADR-021). Opened as [PR #3](https://github.com/AshwinSathian/angularjs-migration-copilot/pull/3) — `git` was initially non-functional in this session's shell (macOS Xcode license not accepted); user accepted it mid-session and the branch/commit/push/PR flow completed normally after.
- **2026-09-15** — PR #3 put through a 4-angle adversarial review (silent-failure, type-design, test-coverage, general) before merge, same practice as M0's. Found and fixed real defects: unvalidated `appName`/`angularCliVersion` were a confirmed, working `ng new` flag-injection and npm alias-injection vector; the subprocess timeout only signaled the immediate process, not the `npm install` grandchild doing the real work; a directory-emptiness check silently treated permission errors as "safe to proceed." All fixed, re-verified with real (not mocked) scaffold + build runs plus real injection-payload rejection checks, 99/99 tests passing — see ADR-022. Merged via [PR #3](https://github.com/AshwinSathian/angularjs-migration-copilot/pull/3) (squash, branch deleted). `docs/milestones/m0.5-scaffold.md` status corrected from stale "not started." Next session: M1 codemods, prompt above.
- **2026-09-15** — M1 started: vendored `CoreUI-AngularJS`/`blur-admin` (ADR-023, PR #5) and implemented codemod pattern #3 with shared `codemods/` plumbing (ADR-024, PR #6). Pattern #3 went through two independent adversarial-review rounds before merge (explicit request to go beyond a single pass) — 8 real defects found and fixed total (ADR-025, ADR-026): a silently-dropped skip reason on partial success, duplicate-class-name collisions (both same-run and pre-existing-binding), an optional-field result type instead of a discriminated union, a `--out` self-overwrite hole, mishandled destructured/default/rest parameters, reversed insertion order for chained calls, classes hoisted out of an enclosing IIFE, and a traversal-order-dependent (rather than source-order) collision winner inside a chain. Both PRs merged after a `docs/decisions.md` merge conflict (both branches appended near the same anchor — resolved by keeping both ADR sequences in order). 96/96 tests passing on `main`. Next session: continue M1 with pattern #1, reusing pattern #3's plumbing rather than re-deriving it — prompt above.
- **2026-09-15** — Pattern #1 ($scope.x = y → class property) implemented (ADR-027), extracting pattern #3's class-wrapping logic into shared `class-wrapping.ts` first, per the prior session's own instruction to reuse rather than re-derive it — confirmed a pure refactor by re-running pattern #3's existing suite unmodified. A real bug was caught by actually running pattern #1 against a realistic file before it even reached review: `$scope.x = y` inside a nested non-arrow function callback was being rewritten to `this.x = y`, but `this` there doesn't refer to the class instance the way `$scope` correctly does via closure. Fixed pre-review. PR #8 then went through two more independent adversarial-review rounds (same practice as pattern #3) — round 2 found the hand-rolled shadow-detection ancestor-walk had three separate holes (a nested `var $scope` shadow, mid-function `$scope` reassignment, getter/setter `this`-binding), the signal to replace it with ts-morph's real symbol resolution instead of patching cases one at a time (ADR-028); also generalized the `this`-rebinding check into `class-wrapping.ts` for future patterns (esp. #10) to reuse. Deliberately deferred: extracting the now-duplicated `RawMatch`/collect-validate-loop structure between the two pattern files, until a third pattern shows the actual shape of the variation. 119/119 tests passing on `main`. Next session: pattern #2 or #9 (both self-contained), prompt above.
- **2026-09-15** — Pattern #2 (controllerAs → component class) implemented (ADR-029), confirming the deferred third-near-duplicate signal from ADR-028: extracted `classWrappingSkipReason` and `collectBareFunctionControllerMatches` into `class-wrapping.ts`, refactoring pattern #1 to use them too (confirmed a pure refactor — its 19 tests passed unmodified before any pattern #2 code was added). Unlike pattern #1, no internal rewriting is needed for controllerAs — the whole function body wraps verbatim. **Actually running pattern #2 against the real vendored fixtures (not just hand-written test fixtures) surfaced that it, and already-merged pattern #1, matched zero real controllers in `blur-admin`/`CoreUI-AngularJS`**: `collectBareFunctionControllerMatches` only recognized an inline function literal, but the dominant real-world idiom (66/91 real `.controller` calls, 0 inline) is `.controller('X', X)` referencing a separately-declared `function X(...) {...}`. Fixed at the shared root via real symbol resolution (ADR-030), which surfaced a second real bug in the fix itself — TypeScript's JS binder records the common `Ctrl.$inject = [...]` annotation as a second "declaration" of the same symbol, which an exactly-one-declaration check rejected as ambiguous. Both fixed and re-verified for real: pattern #1 went from 0/4 to 4/4 hits against `CoreUI-AngularJS`; pattern #2 found 11 real hits against `blur-admin`. **An adversarial code review pass on that very fix (ADR-031) then found the fix itself was broken**: replacing the named `function` declaration with a same-named `class` in place compiles fine only when the declaration happens to precede its `.controller('X', X)` reference — the minority order — and produces a real `tsc` error (`TS2449: Class 'X' used before its declaration`) in the dominant real ordering both fixtures actually use, plus a second real error (`TS2339`) from a `$inject` annotation left dangling on what's now a class. Neither was caught by the test suite, which only asserted substrings, not that the output compiles. Fixed by inserting the class before the call's own statement and deleting the original declaration (and its `$inject` line) instead of replacing in place — re-verified with real `tsc` runs against the actual codemod output for both scenarios, zero relevant errors remaining, all previously-claimed real hits re-confirmed. A third, smaller finding from the same review (`isPlainAssignment` duplicated across two pattern files) was also fixed by hoisting it into `class-wrapping.ts`. **A second, independent review pass on that very fix (ADR-032) found three more real issues, none caught by the first round**: two registrations resolving to the same underlying `function` could each schedule a stale-offset deletion of its span, corrupting unrelated trailing content on collision — confirmed by constructing a file with a marker string that visibly got mangled; fixed by requiring the referenced function's own name to match the registration name plus an independent `claimedNamedDeclarations` dedup set as a second safety net. `hasScopePropertyAssignment` (pattern #2's "already pattern #1's territory" check) didn't apply the same this-rebinding-boundary exclusion pattern #1's own transform uses, so a controller with a safe top-level `this.x = y` alongside an unrelated nested `$scope.y = z` was silently missed by *both* patterns — confirmed by running both codemods against the same file and observing both report no match; fixed. `resolvesToParam`/`resolvesToOneOf` (near-duplicated across the two pattern files) unified into a shared `resolvesUniquelyTo`. Real per-repo hit counts recorded in ADR-030/ADR-031/ADR-032 and above, not smoothed. 158/158 tests passing (7 new regression tests for round 2's findings), `nx run-many -t lint,typecheck,test` clean. Flagged, not fixed: pattern #3 (array-style DI) may have the same named-reference gap for its last-array-element check, and `findInjectAssignmentStatements`' per-match full-file scan is an unoptimized O(matches × file size) — neither investigated/fixed this session, both flagged rather than assumed fine. **Before merge, four review angles (silent-failure-hunter, type-design-analyzer, pr-test-analyzer, general — same practice as M0/M0.5) ran in parallel against the fully-fixed branch (ADR-033)**: found the `$inject`-deletion logic only handled the *first* of multiple annotations for one function (second survived as a real `TS2339`) and, separately, accepted an `$inject` assignment nested inside another expression or a braceless `if` body — deleting the wrong, sometimes much larger range (confirmed corrupting an entire IIFE with a constructed marker-string repro) instead of refusing to touch it. Deeper still: ADR-031's "always declared before its only reference" claim was never actually verified — a `.prototype` extension or second registration referencing the same function before the matched one resurfaces the exact TDZ bug the whole path exists to avoid, confirmed by construction. Fixed together with a new `hasOtherReferences` check that rejects any candidate with an unaccounted-for reference — which also fully subsumes and replaces ADR-032's same-function dedup (two calls sharing a function are now both rejected upstream, a stricter and more correct outcome than "transform the first, skip the second"). Also fixed: a misleading "ambiguous" skip reason the dedup Set produced for a registration that was never a real candidate at all (claim moved to fire only right before a real push, not at first sight of a named match). Two cheap type-design fixes applied (discriminated union for the isNamedDeclaration/injectStatements pairing on its third occurrence; `resolvesUniquelyTo` simplified to array-only). The test-coverage gap behind three of ADR-031's original bugs — nothing ever asserted output actually compiles — closed with a shared `assert-compiles.ts` ts-morph-diagnostics helper, wired into every named-declaration/IIFE/`$inject` test. 165/165 tests passing (7 new), real per-repo hit counts against all three fixture repos unchanged (stricter checks reject nothing in real fixtures, only constructed adversarial cases). PR #10 merged. Next session: pattern #4 or #9, prompt above; the `tsc`-on-output lesson from this session is now enforced by `assert-compiles.ts`, not just a note to remember.
- **2026-09-16** — Resumed per the previous session's own flagged-but-unchecked note: confirmed and fixed pattern #3's (`array-di-to-constructor.ts`) array-last-element check had the same named-reference detection gap ADR-030 found for bare-function `.controller` registrations (ADR-034). Exported and reused `resolveNamedFunctionDeclaration`/`findInjectAssignmentStatements`/`hasOtherReferences` from `class-wrapping.ts` rather than re-deriving them. Actually ran the fix against all 66 registration-bearing files across all three vendored fixtures (not just hand-written test fixtures, the exact discipline ADR-030 established) — zero real array-style DI registrations exist in any of them, so the fix is real and necessary for general correctness but doesn't move pattern #3's fixture hit count (0/66, honestly reported). Round 1 review (`/code-review high`) found the fix itself introduced a real corruption bug for a registration-inside-its-own-body edge case, then traced the same gap to already-merged pattern #1 via the shared `hasOtherReferences` helper — confirmed by actually running the repro against both before fixing at the shared root. Round 2 found no further functional issues, only a docs gap (this session log, ADR-034). **A third review round, run against the open PR before merge per explicit instruction, found round 1's shared-root fix was itself too narrow (ADR-035)**: it only guarded a function referencing *itself* from inside its own body, not a candidate whose body nests a *different* registration referencing a *different* function — confirmed to corrupt output in both the new pattern #3 path and already-merged pattern #1 by actual execution. Fixed with a more general shared check, `findNestedDeletionConflicts`, wired into both `collectBareFunctionControllerMatches` (patterns #1/#2) and pattern #3's own candidate list. Also closed a real test-coverage gap the same round flagged: pattern #2 (`controlleras-to-class.ts`) shares the identical mechanism but had no regression test for either nesting case. **A fourth round — a four-agent `/code-review high` swarm run against the updated PR before merge — converged independently on one more gap in that same fix (ADR-036)**: the nesting-conflict check still only protected a named declaration's deletion range, not an *inline* function/arrow literal's own replaced span, which destroys exactly the same text. Two of the four agents confirmed by direct execution against the actual committed code (one via an isolated scratch snapshot, specifically to rule out interference from this fix already landing concurrently) that an inline outer candidate nesting a second registration silently produced mid-token-sliced corrupted output. Fixed by generalizing the nesting check to cover every candidate's own deleted/replaced range, not just named declarations. A fourth agent explicitly traced through and ruled out, as not a real bug, a structural question another agent had raised (pattern #3's named-declaration branch not requiring the resolved function's own name to match the registration name) — confirmed safe by design, not oversight. Removed a leftover scratch spec file one review agent left behind during its own investigation. **A fifth round — another multi-agent swarm — found one more real, low-severity gap (ADR-037)**: a file whose only real controllerAs/`$scope`-assignment candidate was silently dropped by the nesting-conflict filter reported the same generic "not found" reason as a file with no candidate at all — factually misleading, per CLAUDE.md's reporting-honesty rule, even though low-impact today (the reason string only feeds a CLI message, not M2's future characterization stats). Fixed by having `collectBareFunctionControllerMatches` surface which class names it silently dropped, via a shared `buildNoMatchReason` helper. Two other round-5 findings were investigated and deliberately left as-is, not missed: the array-di named-declaration branch's asymmetric name-match requirement (re-confirmed safe by a second independent agent, a clarifying comment added so it isn't "fixed" into an unnecessary restriction later) and `NestingCheckItem`'s hand-duplicated construction at its two call sites (a real architectural observation, deliberately deferred per this project's own "rule of three" precedent until a third call site shows the actual shape worth generalizing). **A sixth round found one more real bug and one inaccurate comment (ADR-038)**: pattern #3's name-deduplication ran *before* nesting-conflict filtering, so a doomed (nesting-conflicted) candidate still claimed its class name first, wrongly blocking an unrelated, valid later registration sharing that name — confirmed by construction, fixed by deferring dedup to after nesting-conflict filtering, mirroring the order `collectBareFunctionControllerMatches` already used. Separately, `assert-compiles.ts`'s 2683-suppression comment claimed a codemod-emitted class's own members never trigger it — a direct ts-morph probe showed a nested plain-function callback (a common real AngularJS idiom, copied verbatim into emitted class output by patterns #2/#3) does trigger it; the suppression itself stays (same inherent-scope reasoning as 2339), the comment was corrected. PR #11 merged. 180/180 tests passing (14 new across this session, `assertCompiles` used throughout), `nx run-many -t lint,typecheck,test --skip-nx-cache` clean. Not yet done this session: patterns #4/#9, still next up.
- **2026-09-16 (continued)** — After PR #11 merged, user asked to conserve tokens/avoid repeated multi-agent review swarms going forward (saved as a standing preference memory outside this repo). Implemented pattern #4 (simple directive → `@Component`/`@Directive`, ADR-039): checked real fixture shapes *before* writing any detection code (the named-reference idiom ADR-030 found for `.controller` is 100% of real `.directive()` calls too — built for it from the start instead of discovering it the hard way a fourth time), then designed the transform to insert the new class alongside the untouched original rather than delete/replace — a deliberate departure from patterns #1–#3, justified by `@Component`/`@Directive` not being valid AngularJS registration syntax the way a plain class is. This sidesteps pattern #3's entire nesting-conflict bug class by construction rather than by careful guarding. 15 new tests, all passing on first run; ran against all three vendored fixtures for an honest hit count (`angular-phonecat` 0/0, `CoreUI-AngularJS` 0/2, `blur-admin` 3/35) and manually verified every real hit and spot-checked skip reasons against actual fixture source, not just counted. 195 tests passing workspace-wide. Opened as PR #12.
- **2026-09-16 (continued)** — Adversarial review of PR #12, done as a direct self-review (no subagent swarm) per the revised guidance, found two real bugs before merge, both confirmed by direct execution (ADR-040): `getObjectLiteralPropertyValue` conflated "property absent" with "property present but not a plain `key: value` assignment," so an ES6 method-shorthand `controller() {...}` was silently treated as no-controller-at-all — `matched: true`, the controller's real logic dropped entirely, not skipped. Same gap for `template`/`templateUrl`, silently downgrading to `@Directive` and dropping the whole view. Fixed with a helper that distinguishes the two cases, applied where the distinction has real correctness stakes. Also fixed a smaller classification bug in `deriveSelector` (`restrict: 'AC'` was misclassified as element-style). Neither bug was reachable by any real fixture file — both found by adversarially constructing inputs, not by running against the corpus, which stayed unchanged at 0/0, 0/2, 3/35. 3 new regression tests, 198 tests passing workspace-wide, `nx run-many -t lint,typecheck,test --skip-nx-cache` clean. PR #12 merged.
- **2026-09-16 (continued)** — Resumed for pattern #9 (one-way `bindings: { x: '<' }` → `@Input()`). Grepped all three vendored fixtures for real `bindings`/isolate-`scope` shapes before writing any detection code (standing practice per PROGRESS.md's own prompt): zero real `.component()` calls anywhere declare a `bindings` property (angular-phonecat's only two `.component()` calls, the only real `.component()` usage in any fixture, have neither); `blur-admin` has 7 isolate-`scope` directives, all using two-way (`'='`)/interpolated (`'@'`) modes — zero one-way (`'<'`) — and all 7 also have a `link` function, which pattern #4 already unconditionally skips regardless of scope. This directly informed scope: implemented pattern #9 (ADR-041) against `.component()`'s `bindings` only, the pattern's own literal spec definition, and deliberately did not build a `.directive()` isolate-scope path despite ADR-039's deferral — real evidence shows it would transform zero real files in any of the three fixtures either way, and would require duplicating pattern #4's entire DDO/controller/selector machinery to verify against nothing. Reused `resolveNamedFunctionDeclaration` from `class-wrapping.ts` and exported/reused `toPascalCase`/`toKebabCase`/`getObjectLiteralProperty` from `directive-to-component.ts` rather than re-deriving them; same insert-only architecture as pattern #4 (no nesting-conflict machinery needed). Self-review (no swarm, per this session's own standing guidance) specifically checked the new code against ADR-040's exact bug class before it shipped, confirming `bindings`/`controller`/`template`/`templateUrl` all correctly distinguish "absent" from "present but unsupported shape." One real, necessary fix to shared test infrastructure: `assert-compiles.ts` needed a new ignored diagnostic code, `2552` — TypeScript's alternate "cannot find name" code for `Input` (it suggests `oninput` as a close match), the same underlying case `2304` already covers for `@Component`/`@Directive`. Ran the real CLI against both of angular-phonecat's actual `.component()` files to confirm the honest real hit count: `angular-phonecat` 0/2, `CoreUI-AngularJS` 0/0, `blur-admin` 0/0 — reported as-is, not smoothed, consistent with this pattern's own narrow literal definition and how little `.component()` usage exists in these fixtures. 12 new tests, `assertCompiles` used throughout, 210 tests passing workspace-wide, `nx run-many -t lint,typecheck,test --skip-nx-cache` clean. Opened as [PR #13](https://github.com/AshwinSathian/angularjs-migration-copilot/pull/13).
- **2026-09-16 (continued)** — Reviewed the only open PR (#13) adversarially, per explicit instruction to go beyond the basics rather than self-review only. Two independent `/code-review high` passes, each confirmed by direct execution before fixing, found four real bugs (ADR-042): a duplicate `bindings` key emitted two identical `@Input()` fields (`TS2300`, still `matched: true`); a quoted, non-identifier binding key (`'my-attr'`) was emitted verbatim as an invalid class field name; `usedClassNames.add()` ran before `template`/`templateUrl` validation could still fail, permanently claiming a skipped registration's class name and wrongly rejecting a later valid one sharing it — a regression against pattern #4's own already-correct ordering; and the `2552` diagnostic ignore added in ADR-041 was a bare code, too broad (would mask any future near-miss-spelling bug in any codemod's output, not just the one known `Input`/`oninput` case) — narrowed to a message-scoped ignore instead. Also extracted `extractTemplateProp` into `directive-to-component.ts`, closing a DRY gap (`template`/`templateUrl` extraction copy-pasted between the two pattern files) the same review flagged. A third review round after all fixes found nothing further — converged. 5 new tests (`assert-compiles.spec.ts` added as its own file, direct coverage of the new message-scoped ignore, not just incidental through pattern specs), 215 tests passing workspace-wide, `nx run-many -t lint,typecheck,test --skip-nx-cache` clean, real fixture hit counts unchanged. CI green, merged via [PR #13](https://github.com/AshwinSathian/angularjs-migration-copilot/pull/13) (squash, branch deleted). Next session: pick up one of the 5 remaining patterns (#5–#8, #10), prompt above.
- **2026-09-16 (continued)** — Implemented pattern #5 (pure filter → `@Pipe`, ADR-043). Grepped all three vendored fixtures for real `.filter(...)` registrations before writing any detection code (standing practice): 5 real hits, all in `blur-admin`, all using the named-reference idiom already dominant elsewhere — reused `resolveNamedFunctionDeclaration` as-is. Same insert-only architecture as patterns #4/#9, confirmed rather than assumed before committing to it. Identified and handled a correctness issue specific to this pattern before it shipped: the filter's returned function becomes a `transform` *method*, not a constructor body, so a closed-over DI parameter reference needs rewriting to `this.<name>` or it silently breaks — done via `isInsideThisRebindingBoundary`, the same guard pattern #1 already established for a different hazard. Self-review pass found nothing; three independent `/code-review high` rounds run before merge (per explicit "go beyond the basics" instruction) found two real bugs, one per round, before converging clean on the third: round 1 caught a self-recursive named-function-expression factory silently producing an unresolvable self-reference once lifted into a class method (fixed with `hasSelfReference`); round 2 caught `resolvesUniquelyTo` (`class-wrapping.ts`, shared with patterns #1/#2) silently failing to resolve an ES6 object-literal-shorthand closure reference (`{ layoutPaths, input }`) to its declaration — a `matched: true`-but-wrong bug, fixed at the shared root via `ShorthandPropertyAssignment#getValueSymbol()`, confirmed by a probe script and confirmed not to change patterns #1/#2's own existing behavior (traced: neither can ever pass a shorthand-positioned identifier to begin with). The same round also fixed the shorthand rewrite itself, which needs the full `<name>: this.<name>` expansion, not a bare identifier replacement. Real per-repo hit counts: `angular-phonecat` 0/0, `CoreUI-AngularJS` 0/0, `blur-admin` 5/5 — all 5 real hits manually verified via the real CLI. 15 tests, `assertCompiles` used throughout, `nx run-many -t lint,typecheck,test --skip-nx-cache` clean. Opened as [PR #15](https://github.com/AshwinSathian/angularjs-migration-copilot/pull/15). **A fourth review round against the open PR (per explicit "review it adversarially, go beyond the basics" instruction) found a third real bug**: `hasSelfReference` scanned the returned function's own name node as if it were a body reference, so it rejected *every* named returned function as "self-referencing," recursive or not — confirmed by direct execution with a harmless, non-recursive named function. Fixed by excluding the name node itself from the scan; re-confirmed the genuine self-recursion case is still caught. A fifth round converged clean. 16 tests, CI (`secrets-scan`, the only gate per ADR-009) green, merged via [PR #15](https://github.com/AshwinSathian/angularjs-migration-copilot/pull/15). Next session: pick up one of the 4 remaining patterns (#6–#8, #10), prompt above.
- **2026-09-21** — Resumed for pattern #8 (flat `$routeProvider`/ui-router `$stateProvider` states → Angular `Routes` array). Before writing any code, re-verified the prior session's own resume-brief claims against the live fixtures rather than trusting them — one turned out stale: `CoreUI-AngularJS` is **not** 0/0 for routing, it has 27 real `.state()` calls (its own `fixtures/README.md` calls it "heavy ui-router"), correctly 0 real *hits* only because every flat state name is `abstract: true`. Implemented `routes-to-router-config.ts` (ADR-044): resolves the real injected `$routeProvider`/`$stateProvider` parameter via array position or parameter name — deliberately more rigorous than `inventory/scan-routes.ts`'s receiver-text-matching (ADR-019's accepted gap for a read-only report, not a bar for a codemod that emits code) — then walks `.when`/`.state`/`.otherwise` via real symbol resolution, correctly excluding `blur-admin`'s `$urlRouterProvider.when(...)` and an unrelated chained second `.config()` call. Same insert-only architecture as patterns #4/#9/#5. Real per-repo hits: `angular-phonecat` 1/1, `blur-admin` 2/26, `CoreUI-AngularJS` 0/3. Self-review before opening the PR found and fixed one real bug (derived `Routes` constant name never validated as an identifier — a module named `'404'` would have emitted invalid syntax). Opened as [PR #16](https://github.com/AshwinSathian/angularjs-migration-copilot/pull/16). An 8-angle `/code-review high` swarm against the open PR (6 of 8 angles completed — 2 hit an unrelated provider rate limit, not retried, judged sufficient given the other 6's coverage per this project's revised token-economy guidance) found two more real bugs, both fixed with regression tests: **HIGH** — `.otherwise()`'s wildcard entry was emitted at its source position among `.when()` entries rather than forced last; AngularJS evaluates it only after every other route fails to match regardless of where it's written, but Angular's `Routes` array matches top-to-bottom, so an earlier wildcard silently shadowed every route after it — fixed by always appending it last, and warning (not silently dropping) on multiple `.otherwise()` calls (AngularJS overwrites, doesn't accumulate). **MEDIUM** — the provider-parameter lookup only matched by parameter name, missing the real, minifier-safe `fn.$inject = [...]` annotation idiom entirely (not even a skip reason — just silently never recognized as routing) — fixed with `findProviderParamViaInject`, reusing `findInjectAssignmentStatements` rather than re-deriving it. The same rounds drove several DRY fixes: `isTruthyValue` exported from `directive-to-component.ts` (the route file had hand-rolled the identical check and silently diverged on a method-shorthand edge case); `resolveChainRoot` and `arrayDiArityMismatchReason` moved into `inventory/ast-helpers.ts` as shared plumbing, the latter now also used by `array-di-to-constructor.ts` in place of a duplicated message; `collectRouteCalls` now returns pre-narrowed pairs instead of forcing a dead re-check at the call site; a shared `PROVIDER_KIND` lookup replaces two copies of a ternary; the derived routes-constant name is validated once instead of twice. One pre-existing gap shared with patterns #4/#9/#5 was flagged, not fixed (see "Known, open, non-blocking"): `hasExistingTopLevelBinding` only checks file-top-level bindings, not the enclosing IIFE an insertion actually lands in. 20 tests, `assertCompiles` used throughout, 251 tests passing workspace-wide, `nx run-many -t lint,typecheck,test --skip-nx-cache` clean, real fixture hit counts unchanged after every fix. A second aggressive adversarial `/code-review` round (explicit user request to go further) found 4 more real test-coverage gaps (documented skip paths with no regression test — non-object-literal route config, missing/non-literal `.state()` `url`, non-string-literal `otherwise()` argument, non-string-literal name/path) and a stale test-count claim in this very entry (was reporting "21/244", actually 20/251 at the time) — all fixed. `gh pr merge` was initially declined by this session's auto-mode classifier ("Merge Without Review," an unattended-merge guard, not a finding about the PR itself) — merged once the user explicitly asked for it. See the next log entry for a further review round the user requested after this merge.
- **2026-09-21 (continued)** — User explicitly asked for an even more aggressive adversarial review of the now-merged PR #16, before handing off to a new session. A second `/code-review high` swarm against the merged code (not the PR — merge had already happened) found: (1) 4 real documented-skip-path test-coverage gaps in `routes-to-router-config.spec.ts` (non-object-literal route config, missing/non-literal `.state()` `url`, non-string-literal `otherwise()` argument, non-string-literal name/path) — closed, plus a stale test-count claim in the docs themselves ("21/244" vs. the actual 16/247 at the time) — corrected to accurate numbers throughout. (2) A real, confirmed-by-execution bug in already-merged shared infrastructure (`class-wrapping.ts`'s `hasExistingTopLevelBinding`), affecting every insert-only pattern (#4/#9/#5/#8), not just #8 — see **ADR-045** for the full writeup: the collision check only looked at the source file's direct-child level, never the enclosing IIFE block `nearestInsertionPointStart` actually inserts into, so a same-named pre-existing binding inside that IIFE went undetected — one repro produced a silently-wrong `component:` reference (`matched: true`, no warning), the other a real `TS2451` redeclaration error. Fixed by broadening the check to the whole file. (3) A smaller ordering bug in pattern #8 itself: the routes-constant-name collision check was computed early but only acted on after wastefully walking every route call, also producing misleading combined warnings — fixed by skipping the whole registration immediately once its name is known to be doomed. (4) While re-verifying real fixture hit counts for every insert-only pattern as part of this round's own discipline (not something any review agent specifically went looking for), found pattern #5's own already-merged `angular-phonecat` hit count (ADR-043, PR #15) has been wrong since it was first reported: actually `1/1`, not `0/0` — `checkmark.filter.js` is a real, in-scope filter the codemod already transforms correctly; the original evidence-gathering grep just couldn't see past this fixture's line-broken method-chaining style. Corrected in `docs/PROGRESS.md`'s "Where things stand" (ADR-043's own decisions.md entry is left as originally written, per the append-only rule — the correction lives in ADR-045 instead). Since this fix touches shared code, the full workspace suite was re-run (unchanged, no regressions) and real fixture hit counts re-verified unchanged for all four insert-only patterns after the fix, not just pattern #8's own. 22 tests in `routes-to-router-config.spec.ts` (2 more, the two IIFE-scope collision repros), 253 tests passing workspace-wide, `nx run-many -t lint,typecheck,test --skip-nx-cache` clean. Landed as its own small PR (branched from `main` after PR #16's merge, since the bug was found post-merge) and merged. Next session: pick up one of the 3 remaining patterns (#6, #7, #10), prompt above — and per this session's own lesson, re-verify any inherited hit-count/fixture-evidence claim against the real CLI before trusting it, not just against a text grep.
- **2026-09-21 (continued)** — Resumed for pattern #10 ($emit/$broadcast/$on within one module → typed RxJS `Subject` service), the last of the 3 patterns flagged as remaining. Grepped all three vendored fixtures for real `$emit`/`$broadcast`/`$on` calls before writing any detection code (standing practice): found only 9 raw occurrences (2 in `CoreUI-AngularJS`, 7 in `blur-admin`), and on inspection every single one is either a `$destroy` lifecycle-cleanup listener, a ui-router-internal `$stateChangeSuccess`/`$stateChangeStart` broadcast, or one orphaned `$broadcast('fileProgress', ...)` with no listener anywhere — zero real pairs of the pattern's actual target shape (an event name both sent and received by app code). Given this near-total absence of real-fixture evidence, plus this pattern's genuine architectural divergence from every prior insert-only pattern (no single registration call to attach to; a *complete* migration would also mean rewriting scattered call sites, not just inserting alongside one), asked the user directly how to scope it rather than guessing — confirmed: insert-only scaffold (one `EventBusService` per file with a typed `Subject` per paired event), no call-site rewriting. Implemented `event-bus-to-subject.ts` (ADR-046). **A first adversarial review round found one severe, confirmed-by-execution bug self-review had missed entirely**: reusing `nearestInsertionPointStart` (class-wrapping.ts) by analogy to every prior pattern landed the generated class *inside* whatever closure happened to contain the (often deeply-nested) matched call — e.g. inside a `$scope.notify = function () {...}` closure, redeclared per invocation, unreachable, still `matched: true`. Every self-written test had happened to use a fixture where the matched call sat directly at the IIFE's top level, so self-review's own suite never exercised the real bug. Fixed with a new pattern-local `moduleTopLevelInsertPos`: `EventBusService` has no constructor parameters and closes over nothing, so unlike every prior pattern's class it has no need to stay inside an enclosing IIFE — it's now always inserted at true file-top level, after any leading `'use strict'` directive-prologue statement. **A second adversarial round on the fixed code found two more real bugs**: the reserved-word check (`isValidClassName`) was wrongly applied to a derived member-name base that's never emitted bare (fixed by checking the character-class-only `VALID_IDENTIFIER` regex instead — `deleteSubject`/`emitDelete` are valid even though `delete` alone is reserved); and the collision check only guarded the derived `EventBusService` class name, never `Subject`/`Injectable` (the names the generated class body actually references), so a pre-existing top-level binding with either name would silently shadow the real rxjs/`@angular/core` names — fixed by checking all three. Regression tests added for both plus the missing IIFE-scoped collision case and a leading-digit-identifier skip case. Real per-repo hit counts, honestly reported and re-verified via the real CLI against every one of the 8 real files with a matching call, both before and after every fix: `angular-phonecat` 0/0, `CoreUI-AngularJS` 0/2, `blur-admin` 0/7 — an honest 0/0/0, same accepted-low-number precedent as pattern #9's own 0/0/0/0 (ADR-041). 18 tests, `assertCompiles` used throughout, 271 tests passing workspace-wide, `nx run-many -t lint,typecheck,test --skip-nx-cache` clean. Flagged, not fixed: `cli.ts`'s shared warning label is mildly inaccurate for this pattern's own skip reasons (pre-existing generic wording, out of scope for a single-pattern change); template-literal event names and non-ASCII identifier sanitization both unrecognized/lossy, zero real-fixture evidence for either. Only 2 patterns remain for M1 (#6, #7). Opened as [PR #18](https://github.com/AshwinSathian/angularjs-migration-copilot/pull/18).
- **2026-09-21 (continued)** — User asked for CI to finish, then an aggressive adversarial review of PR #18, explicitly "go beyond the basics." `/code-review ultra` against the actual PR diff found two more real, confirmed-by-execution bugs neither of the first two self-run rounds had caught: (1) detection matched any `x.$emit(...)`/`x.$broadcast(...)`/`x.$on(...)` call by method name alone, no receiver check — confirmed to false-positive on a hand-rolled object exposing same-named methods with nothing to do with AngularJS scope semantics, `matched: true`. Fixed by restricting to the exact receiver names every real fixture actually uses (`$scope`/`$rootScope`/`scope`/`rootScope`) — deliberately not full DI/symbol resolution, same "skip when ambiguous" conservatism as the rest of this pattern family, no real evidence to justify more. (2) `hasExistingTopLevelBinding`'s (class-wrapping.ts) `Subject`/`Injectable`/`EventBusService` collision check never looked at `import` bindings, only function/class/variable declarations — confirmed a pre-existing `import { Subject } from './my-custom-subject';` silently shadowed the real rxjs `Subject` the emitted code depends on, `matched: true`, no compile diagnostic catches it. Fixed at the shared root (a new `importLocalBindingNames` helper covering default/namespace/named imports), the same failure class ADR-045 already fixed for this exact function's IIFE-scope blind spot — a different blind spot in the same shared function, affecting every insert-only pattern, not just #10. Full workspace suite re-run clean after the shared-infra change; real fixture behavior re-verified unchanged for pattern #4 (still 3/3 real hits) and pattern #10 (still 0/8) — no vendored fixture uses ES `import` syntax, so the new check only adds detection capability, doesn't change any existing outcome. 4 new regression tests, 22 total in `event-bus-to-subject.spec.ts`, 275 tests passing workspace-wide, `nx run-many -t lint,typecheck,test --skip-nx-cache` clean. `docs/decisions.md` (ADR-046, extended in place — still the same open, unmerged PR, same precedent as prior patterns folding multiple pre-merge rounds into one ADR entry) and this file updated. Merged PR #18. Next session: pick up pattern #6 or #7, prompt above.
- **2026-09-21 (continued)** — Resumed for pattern #6 ($http + .then() chains, depth ≤ 2, → HttpClient + RxJS), the last of the 2 patterns flagged as remaining before this session besides #7. Grepped all three vendored fixtures for real `$http` usage before writing any detection code (standing practice): `CoreUI-AngularJS` has zero references; `angular-phonecat` fetches data exclusively through `$resource` (`phone.service.js`), never `$http` directly, confirmed its only `$http` mention anywhere is a `$httpBackend` mock inside a Karma spec file, not app code; `blur-admin` has exactly 2 real `$http` call sites, both in one file (`WeatherCtrl.js`) — a config-object `$http({method,url,params}).then(success,error)` (with `method`/`url` bound through local `var`s, not literals) and a `$http.jsonp(url).then(success,error)`. Both success/error callbacks are inline named function expressions with bodies heavily entangled with `$scope` and sibling closure functions (`saveWeatherData`, `makeChart`) that have no safe way to be lifted out. Given this real entanglement, a genuine step up in risk from every prior pattern, asked the user directly how to scope it before writing any code (same practice as pattern #10's own scope confirmation) — confirmed: a signature-only scaffold, generating a typed `HttpClient`-based method for each qualifying call but never copying/rewriting the `.then()`/`.catch()` callback bodies at all. Implemented `http-then-to-httpclient.ts` (ADR-047), reusing pattern #10's own insert-only/module-top-level-insertion architecture (the generated class has no closure dependency, so no need to stay inside an enclosing IIFE) and `hasExistingTopLevelBinding`'s now-import-aware collision check (ADR-046) for free. **Self-review, before any PR was opened, found and fixed two real bugs**: the derived URL was embedded into the generated string literal with no escaping — a URL containing a single quote produced a real syntax error, confirmed by direct execution, fixed with `escapeSingleQuoted`; and `resolveStaticString`'s variable resolution never checked whether a resolved `method`/`url` variable is reassigned anywhere else in the file (`getDeclarations()` only reports declaration sites) — `var method = 'GET'; method = 'POST';` silently resolved to `'GET'`, a `matched: true`-but-wrong bug, fixed by rejecting any candidate with a plain-assignment reference anywhere in the file. Real per-repo hit counts, honestly reported and confirmed via the real CLI against every file mentioning `$http` across all three fixtures: `angular-phonecat` 0/0, `CoreUI-AngularJS` 0/0, `blur-admin` 1/2 (`updateWeather` scaffolded; the `jsonp` call correctly surfaced as a documented skip warning, not silently dropped). 16 tests, `assertCompiles` used throughout, 291 tests passing workspace-wide, `nx run-many -t lint,typecheck,test --skip-nx-cache` clean. Only pattern #7 remains for M1 — once it lands, M1 is done and M2 (verification gate) is next.
- **2026-09-21 (continued)** — User asked for CI to finish, then an aggressive adversarial review of PR #19, explicitly "go beyond the basics" (same instruction pattern as PR #18). `/code-review ultra` against the actual PR diff found five more real, confirmed-by-execution bugs neither self-review round had caught: `hasParamsConfig` misread a body-bearing verb's 2-argument call (`post(url, data)`, no real config object) as having a trailing `params` config whenever the body literal happened to contain a `params` key, silently splitting real POST body data into a separate caller-supplied argument — fixed by requiring the right minimum argument count per verb before treating the last argument as config. The reassignment guard from the first self-review round only recognized plain `=` reassignment, not compound operators (`var url = '/api/users'; url += '/extra';` still resolved to the stale value) — fixed to check every assignment-family operator. A derived method name colliding with the generated class's own `http` constructor parameter or the reserved `constructor` member name broke compilation outright (`TS2300`/`TS2392`, confirmed via `assertCompiles`) — fixed with a reserved-name check. `body: unknown` was added to the generated signature purely from the HTTP verb, forcing a real no-payload trigger POST to expose a meaningless body parameter to callers — fixed by tracking whether the original call actually had body data (mirroring how `hasParams` already worked), passing `null` positionally when a body-bearing verb genuinely has none, since `HttpClient.post`'s real signature requires *some* body argument regardless. And the scaffolded `params?: Record<string, unknown>` parameter type isn't assignable to Angular's real `HttpClient` params option type — invisible to this project's own `assertCompiles` (which never resolves `HttpClient` at all) but a real `tsc` error in an actual Angular project — fixed by narrowing to `Record<string, string | number | boolean>`. 8 new regression tests (21 total in `http-then-to-httpclient.spec.ts`), 296 tests passing workspace-wide, `nx run-many -t lint,typecheck,test --skip-nx-cache` clean, real fixture hit count re-verified unchanged (`blur-admin` still 1/2). `docs/decisions.md` (ADR-047, extended in place — same open-PR precedent as ADR-046) and this file updated. M1's last remaining pattern is #7.
- **2026-09-21 (continued)** — A fourth review round on PR #19, run against the already-fixed code before merge (same "keep going until it converges" pattern as PR #18's own history), found two more real, confirmed-by-execution bugs: `hasBody` was computed from a `data` config-property's presence alone, not from whether the verb was actually body-bearing — a config-object GET with a stray `data` key (legal, if nonstandard, AngularJS) scaffolded a `body: unknown` parameter that was declared but never passed to `this.http.get(...)`, silently dropping anything a caller supplied there. Fixed by requiring both the verb being body-bearing and `data` being present. Separately, the "no safe method name" skip message always rendered in shorthand syntax (`$http.<method>('<url>')`) regardless of whether the actual match came from the config-object or shorthand form, producing a diagnostic string that doesn't match anything present in the file a developer would go looking at. Fixed with a `sourceKind` field on each match and a message renderer reflecting the real source syntax. 2 new regression tests (23 total in `http-then-to-httpclient.spec.ts`), 298 tests passing workspace-wide, `nx run-many -t lint,typecheck,test --skip-nx-cache` clean, real fixture hit count re-verified unchanged (`blur-admin` still 1/2). `docs/decisions.md` (ADR-047, extended in place) and this file updated. **Correction**: a fifth review round (next entry) ran before this actually merged — PR #19 was not yet merged at the point this entry was originally written; see below for the real merge point.
- **2026-09-21 (continued)** — A fifth review round on PR #19, run against the already-fixed code before merge, found six more real, confirmed-by-execution bugs (all fixed) plus two narrow gaps deliberately flagged rather than fixed: `escapeSingleQuoted` didn't escape line-terminator characters (`\n`/`\r`/U+2028/U+2029), only `\`/`'` — a URL resolving to a string containing an actual newline broke the emitted string literal (a real `TS1002`); fixed, with an ironic self-inflicted repeat of the same bug class mid-fix (the first attempt embedded literal U+2028/U+2029 characters into the regex source instead of escape sequences, breaking the file's own regex literal — caught immediately by re-running the test suite before it went anywhere). Both URL-resolution checks used `if (!url)` instead of checking for `undefined` specifically, wrongly rejecting a legally-resolved empty-string URL. `findEnclosingFunction` didn't stop at method/accessor declaration boundaries the way `isInsideThisRebindingBoundary` (class-wrapping.ts) already does elsewhere in this codebase, so a call inside an ES6 method-shorthand property was invisible to it and got attributed to an unrelated outer function instead. An unrecognized-but-genuinely-chained shorthand verb (a plausible typo like `.remove(...)`) surfaced no skip reason at all, falling back to a factually wrong "not found" message. The config-object form didn't recognize `method: 'JSONP'` the way the shorthand `.jsonp()` form did, despite AngularJS documenting it as the config-object equivalent. `RESERVED_METHOD_NAMES` hardcoded `'http'` independently of the actual emitted constructor-parameter name — not a live bug today, but a real single-source-of-truth gap; unified into one `HTTP_PARAM_NAME` constant. Flagged, not fixed (zero real-fixture evidence for either): a destructuring/`for`-loop reassignment isn't caught by the reassignment guard; `chainLinkCount` doesn't see through a `ParenthesizedExpression`. 5 new regression tests (28 total in `http-then-to-httpclient.spec.ts`), 303 tests passing workspace-wide, `nx run-many -t lint,typecheck,test --skip-nx-cache` clean, real fixture hit count re-verified unchanged (`blur-admin` still 1/2, `angular-phonecat`/`CoreUI-AngularJS` still 0/0). `docs/decisions.md` (ADR-047, extended in place a second time) and this file updated. Merged PR #19 after this round. M1's only remaining pattern is #7 — once it lands, M1 is done and M2 (verification gate) is next.
