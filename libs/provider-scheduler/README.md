# libs/provider-scheduler

Job-level, rate-limit-aware scheduling across Groq, Gemini, and OpenRouter. Standalone — no Mongo dependency at this layer; M3 ships a file-backed version, promoted to Mongo-backed once M4 builds that layer. See [docs/product-spec.md §7](../../docs/product-spec.md).

Not built yet. See [M3](../../docs/milestones/m3-llm-fallback.md).
