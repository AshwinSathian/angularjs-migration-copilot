# libs/shared-types

DTOs shared between `apps/api`, `apps/web`, and `libs/migration-core` — the `migrationJobs`/`fileResults`/`providerUsage` shapes from [docs/product-spec.md §8](../../docs/product-spec.md), kept in one place so the three consumers can't drift out of sync with each other.

Not built yet.
