# libs/secrets-scan

Stage 0's redaction pass — standalone and unit-testable in isolation. Runs before any file content reaches an LLM provider, in both CLI and hosted mode, regardless of who's paying for the API call. See [docs/product-spec.md §6.1](../../docs/product-spec.md).
